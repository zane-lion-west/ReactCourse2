import JokeComponent from './JokeComponent/JokeComponent';

export { JokeComponent };

/*

  JavaScript Functions:

  function myFunction() {

  }

  const myFunction = () => {
    run any code here
    mostly used for running multiple lines of code
    return any value if you like
  }

  () => (run a single statement and the value of that statement will be returned)

  statement = any thing you can run in 1 line
    () => myFunction() // The return value of myFunction() will be returned
    () => showElement // will return the variable showElement
    () => (showElement ? <Element /> : undefined) // will return <Element /> if showElement is `true` else will return undefined

*/

/*

  Javascript Comments:

  Single Line: 
  // This is a single line comment

  Multiple Line: 
  /* and then end with the thing at the bottom

  JSDoc Comment:
  /** then end with the thing at the bottom (only one star on the closing tag)
  
  
*/
